# App Engine configuration (Alternative to Cloud Run)
runtime: python311

env_variables:
  PORT: 8080

instance_class: F2

automatic_scaling:
  min_instances: 1
  max_instances: 10
  target_cpu_utilization: 0.6

handlers:
  - url: /.*
    script: auto
