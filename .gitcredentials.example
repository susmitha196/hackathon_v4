# Git Credentials Example
# DO NOT commit this file with real credentials!
# 
# To use Git credential storage:
# 1. Copy this file to .gitcredentials (it's already in .gitignore)
# 2. Add your credentials in this format:
#    https://USERNAME:TOKEN@collaborating.tuhh.de
#
# Or use Git credential helper (recommended):
#   git config --global credential.helper store
#   Then on first push, enter credentials when prompted
#   They will be saved in ~/.git-credentials (Windows: %USERPROFILE%\.git-credentials)
#
# For TUHH GitLab, use Personal Access Token instead of password:
#   https://collaborating.tuhh.de/-/user_settings/personal_access_tokens
